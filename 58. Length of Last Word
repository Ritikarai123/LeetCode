class Solution {
    public int lengthOfLastWord(String s) {
        int count=0;
        String r=s.trim();
      
       for(int i=r.length()-1;i>=0;i--){
           if(r.charAt(i) == ' ')
          break;
           else
           count++;
       }
       return count;
    }
}
